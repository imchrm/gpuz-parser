# TODO.md — goldenpages-scraper

## Статус: не начато

---

## Фаза 0 — Разведка и разблокировка [BLOCKER]

Все пункты этой фазы должны быть выполнены **до написания любого кода**.
Результаты зафиксировать в CONTEXT.md (раздел "Обновления по итогам разведки").

- [ ] **0.1** Проверить `robots.txt`:
      GET `https://www.goldenpages.uz/robots.txt`
      Убедиться, что `/rubrics/`, `/company/`, `/city/`, `/search/` не запрещены.
      Особое внимание — страница поиска уже имеет `noindex, nofollow` в meta.

- [ ] **0.2** [CRITICAL] Установить механизм пагинации рубрик.
      Открыть в браузере рубрику с > 15 компаний (например `/rubrics/?Id=3473`).
      Прокрутить вниз, найти элемент пагинации или кнопку "Показать ещё".
      Зафиксировать: является ли пагинация серверной (GET-параметр) или
      клиентской (JavaScript/AJAX). Если клиентская — поднять вопрос об
      использовании Selenium/Playwright вместо requests.

- [ ] **0.3** [CRITICAL] Исследовать кнопку "Скачать список" на странице рубрики.
      Нажать на иконку Excel рядом с `"Скачать список"` в браузере.
      Зафиксировать: возвращает ли она файл без авторизации, какой формат
      (xlsx/csv), какие поля содержит, есть ли телефоны. Если скачивание
      работает без авторизации и содержит телефоны — это предпочтительный
      способ сбора данных вместо парсинга HTML.

- [ ] **0.4** Установить параметры формы поиска.
      Открыть `/search/`, заполнить форму (вид деятельности + город),
      нажать "Искать". Зафиксировать URL результирующей страницы и имена
      POST-параметров через DevTools (вкладка Network).

- [ ] **0.5** Проверить стабильность FAQ-блока с телефонами.
      Открыть исходный HTML 15-20 разных компаний (малых, крупных,
      с одним телефоном, с несколькими). Убедиться, что паттерн
      `"Номер телефона ... - XX XXXXXXX"` присутствует стабильно.
      Зафиксировать все варианты форматирования номеров.

- [ ] **0.6** Исследовать ссылки через `/go/?u=HASH`.
      Выяснить: можно ли получить целевой URL сайта компании без перехода
      по редиректу? Проверить наличие target URL в атрибуте `title` ссылки
      или в `data-`атрибутах. Если нет — решить, нужно ли делать HEAD-запрос
      для резолвинга редиректа.

- [ ] **0.7** Собрать таблицу ID городов и регионов.
      Открыть главную страницу и выпадающий список городов/областей.
      Зафиксировать соответствие: "Ташкент" -> `?Id=296`, "Ташкентская область"
      -> `?Id=N`, и т.д. Добавить в CONTEXT.md.

---

## Фаза 1 — Структура проекта

- [ ] **1.1** Создать структуру директорий согласно ARCHITECTURE.md.

- [ ] **1.2** Создать `pyproject.toml` (или `requirements.txt`) с зависимостями:
      - `requests` (или `httpx[http2]` — если выбран async-режим по итогам 0.2)
      - `beautifulsoup4`
      - `lxml`
      - `pydantic >= 2.0`
      - `openpyxl`
      - `tenacity`
      - `rich` (опционально)
      - Если пагинация клиентская: `playwright` или `selenium`

- [ ] **1.3** Создать `src/models.py` — Pydantic-модели
      (`Company`, `SearchParams`, `ScraperResult`). Строгая типизация.

- [ ] **1.4** Создать `src/config.py` — константы:
      базовый URL, задержки, User-Agent, известные ID городов/регионов,
      пути вывода.

---

## Фаза 2 — HTTP-клиент и базовый парсер

- [ ] **2.1** Реализовать `src/http_client.py`:
      - `fetch_page(url: str) -> str`
      - Retry через `tenacity` (3 попытки, экспоненциальная задержка)
      - Случайная пауза между запросами (1.5–3.5 сек)
      - Сессия с cookies (сайт использует CSRF-сессию)
      - Заголовок `User-Agent` приближен к браузерному
      - Опционально: `fetch_json(url: str, data: dict) -> dict`
        для POST-запросов поиска

- [ ] **2.2** Реализовать `src/parsers/company_parser.py`:
      - `parse_company_page(html: str, company_id: int) -> Company`
      - Извлечение всех полей из HTML
      - Телефоны: парсить FAQ-блок, паттерн `"Номер телефона ... - NN NNNNNNN"`
        с разделением по `;`
      - Сайт: резолвинг хеша `/go/?u=HASH` через HEAD-запрос
        (или извлечение из `title`-атрибута, если доступен)
      - Отсутствующие поля возвращать как `None`, не бросать исключения

- [ ] **2.3** Реализовать `src/parsers/listing_parser.py`:
      - `parse_company_ids(html: str) -> list[int]`
        Извлечение числовых ID компаний из ссылок `/company/?Id=N`
      - `get_total_count(html: str) -> int`
        Парсит "Найдено организаций: N"
      - `get_next_page_url(html: str, current_url: str) -> str | None`
        Ссылка на следующую страницу (реализовать после установления
        механизма пагинации в задаче 0.2)

---

## Фаза 3 — Альтернативный путь: Excel-экспорт [зависит от 0.3]

**Если задача 0.3 подтвердила работоспособность "Скачать список":**

- [ ] **3A.1** Реализовать `src/collectors/excel_downloader.py`:
      - `download_rubric_excel(rubric_id: int) -> list[Company]`
      - Скачать Excel-файл через GET/POST-запрос
      - Распарсить с помощью `openpyxl`
      - Смапировать колонки на модель `Company`

**Если "Скачать список" требует авторизации или не содержит телефонов:**
перейти к Фазе 3B.

---

## Фаза 3B — Сборщики через HTML-парсинг [если 3A не применима]

- [ ] **3B.1** Реализовать `src/collectors/rubric_collector.py`:
      - `collect_rubric(rubric_id: int) -> list[Company]`
      - Итерация по всем страницам пагинации
      - Для каждого ID компании: GET + парсинг страницы компании

- [ ] **3B.2** Реализовать `src/collectors/city_collector.py`:
      - `collect_city(city_id: int) -> list[Company]`

- [ ] **3B.3** Реализовать `src/collectors/keyword_collector.py`:
      - `collect_keyword(keyword: str, city_id: int | None) -> list[Company]`
      - POST-запрос к `/search/` с параметрами формы

- [ ] **3B.4** Реализовать `src/collectors/multi_rubric_collector.py`:
      - `collect_rubrics(rubric_ids: list[int]) -> list[Company]`
      - Дедупликация по `company_id`

---

## Фаза 4 — Экспорт

- [ ] **4.1** Реализовать `src/exporters/csv_exporter.py`:
      - Кодировка `utf-8-sig` (UTF-8 с BOM для Windows Excel)
      - Разделитель `;`
      - Списки (телефоны, виды деятельности) объединять через ` | `

- [ ] **4.2** Реализовать `src/exporters/excel_exporter.py`:
      - `openpyxl`, лист `Companies`
      - Заголовки: жирный шрифт, заморозка первой строки
      - Автоширина колонок

---

## Фаза 5 — CLI-интерфейс

- [ ] **5.1** Реализовать `main.py`:
      - `--rubric ID` (повторяется для нескольких рубрик)
      - `--city ID`
      - `--keyword СЛОВО`
      - `--output PATH`
      - `--format csv|xlsx|both`
      - `--limit N` (для тестирования)
      - `--delay-min`, `--delay-max`

---

## Фаза 6 — Тестирование

- [ ] **6.1** Сохранить fixture HTML: страницы компании (крупная/малая/без телефона),
      страница рубрики.

- [ ] **6.2** Написать `tests/test_company_parser.py` — покрытие всех полей.

- [ ] **6.3** Написать `tests/test_listing_parser.py`.

- [ ] **6.4** Тестовый прогон: одна рубрика, `--limit 5`, проверить CSV/Excel.

---

## Известные риски и открытые вопросы

| ID   | Вопрос / риск                                                            | Приоритет | Статус       |
|------|--------------------------------------------------------------------------|-----------|--------------|
| R-01 | Пагинация рубрик — механизм неизвестен, возможен JS                     | CRITICAL  | Открыт (0.2) |
| R-02 | "Скачать список" — доступность без авторизации и формат не проверены    | HIGH      | Открыт (0.3) |
| R-03 | Телефоны в FAQ-блоке — стабильность разметки не проверена на выборке    | HIGH      | Открыт (0.5) |
| R-04 | robots.txt не прочитан                                                   | HIGH      | Открыт (0.1) |
| R-05 | Параметры POST-формы поиска неизвестны                                   | MEDIUM    | Открыт (0.4) |
| R-06 | URL сайтов компаний скрыты за /go/?u=HASH — резолвинг через редирект   | MEDIUM    | Открыт (0.6) |
| R-07 | ID городов и регионов не собраны систематически                          | MEDIUM    | Открыт (0.7) |
| R-08 | Если пагинация через JS — весь стек меняется на Playwright               | HIGH      | Зависит от R-01 |
